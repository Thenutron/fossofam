# Neon injects this automatically when you install the Vercel-Neon integration.
# For local dev, copy the connection string from the Neon dashboard (or run `vercel env pull`).
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
